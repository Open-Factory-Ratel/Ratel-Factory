{
  "name": "ratel",
  "version": "0.1.0",
  "description": "Ratel — AI Software Factory",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc && node -e \"require('fs').cpSync('src/observatory/dashboard.html','dist/observatory/dashboard.html')\"",
    "build:all": "npm run build && npm run build --workspace=packages/core && npm run build --workspace=packages/opencode-plugin && npm run build --workspace=packages/pi-extension",
    "dev": "tsx src/adapters/pi-sdk/main.ts",
    "start": "node dist/adapters/pi-sdk/main.js",
    "test": "tsx --test test/**/*.test.ts",
    "test:all": "npm test && npm test --workspace=packages/core && npm test --workspace=packages/opencode-plugin && npm test --workspace=packages/pi-extension"
  },
  "dependencies": {
    "@earendil-works/pi-ai": "^0.79.0",
    "@earendil-works/pi-coding-agent": "^0.79.0",
    "@sinclair/typebox": "^0.34.49",
    "pi-orch-extension": "^1.0.0"
  },
  "devDependencies": {
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
