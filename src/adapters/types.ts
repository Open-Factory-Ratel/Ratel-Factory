/**
 * Ratel Adapter Interface Types
 *
 * Defines the minimal contract between Ratel core and communication adapters.
 * Each adapter (Pi SDK, Codex, Opencode, Claude Code) implements this interface.
 * Core modules import from this interface, not from concrete adapter implementations.
 */

// TODO: Define adapter interface for multi-platform support
// For now, this file exists as a placeholder for the future adapter layer.
// Current adapter (Pi SDK) lives in adapters/pi-sdk/.
